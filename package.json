{
  "name": "purescript-by-exampled",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "bower": "^1.8.0",
    "pulp": "^10.0.1",
    "purescript": "^0.10.7"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
